import "styled-components";

declare module "styled-components" {
    export interface DefaultTheme {
        body: string;
        text: string;
        borderRadius: string;
        padding: string;
        margin: string;
        green: string;
        red: string;
        gray: string;
    }
}
